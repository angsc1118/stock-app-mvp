# ==============================================================================
# 檔案名稱: utils.py
# 
# 功能: 存放跨頁面的共用 UI 元件 (如: 側邊欄狀態列)
# ==============================================================================

import streamlit as st

def render_sidebar_status():
    """
    渲染側邊欄頂部的全域狀態列 (Global Status Bar)
    包含：最後更新時間、分隔線
    """
    with st.sidebar:
        # 取得 Session 中的更新時間
        update_time = st.session_state.get("price_update_time", None)
        
        if update_time:
            # 使用綠色圓點代表已連線/有資料
            st.caption(f"🟢 資料時點: {update_time}")
        else:
            # 使用灰色圓點代表未初始化
            st.caption("⚪ 資料時點: 尚未更新")
            
        # 統一的分隔線，區隔下方的「核心操作區」
        st.divider()
